server:
  port: 8180
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
spring:
  application:
    name: dashboard-hystrix

hystrix:
  dashboard:
    proxy-stream-allow-list: "localhost"